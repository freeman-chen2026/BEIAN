# 每日通航运行情况跟踪表生成器

**使用方式**：
1. 上传 `航段数据导出 (xxx).xlsx`（数据源）
2. 上传 `每日通航运行情况跟踪表（天成商务航空有限公司）xxxxxx.xlsx`（带格式的模板）
3. 下载生成的文件，所有样式（底色、字体、边框）均保留，日期格式为 `2026-07-07 00:00:00`

**特点**：
- 完全基于模板填充，不改变原有格式
- 自动映射机型代码（ICAO）
- 自动填入“3.公务航空运行”
- 根据“航段状态”自动判断是否落地，并填入实际结束时间
